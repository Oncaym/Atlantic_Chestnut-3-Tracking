{
  "type": "commonjs",
  "description": "Local override — dxf-cli.js uses CommonJS (require); the repo root package.json sets \"type\":\"module\" for the Vercel API functions, so this file scopes CommonJS back on for just takeoff/tools/."
}
